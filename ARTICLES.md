# Articles I want to write

## datacenters in space

Get the math for speed of light latency and bandwidth

Move away from availability zones but move to latency boundaries. Eg deploy this within a 10ms latency boundary around Amsterdam.

Databases should adapt their synchronization to replicate within this latency boundary. This means you can prioritize data replication according to the sattelites positioning, having full replication across the globe in a later stage

## Abstractions that people get wrong, SNI, DNS, HTTP Host names

## My year health yourney

## My thoughts about AI

I was originally thinking about 'context' and 'validation'. I now wonder if i need to include 'tools'. Also, validation is basically a combination of 'context' and 'tools', so maybe need to collapse them? Perhaps 'Context' and 'Capabilities' is the better abstraction.

Also, to make an analogy to robots, they talk about 'actuators' aka tools, and sensors aka context. But context is also more, it's a 'goal'.

What is the source of truth abstractions to think about that will be valid in 10 years?
